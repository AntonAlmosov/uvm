overwrite: true
schema: "http://localhost:1337/graphql"
documents: "model/gql/*.{tsx,ts}"
generates:
  model/api.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
config:
  withHooks: true
